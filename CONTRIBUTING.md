# Contributing

1. 先閱讀 `AGENTS.md` 與相關規格。
2. 每個 PR 聚焦單一功能或修正。
3. 資料模型變更必須同步更新 TypeScript、Rust、JSON Schema、sample project 與 migration 設計。
4. 不接受會直接刪除使用者素材的預設行為。
5. 提交前執行前端型別檢查、測試與 Rust workspace 測試。
6. UI 變更需附桌面與窄視窗畫面，確認鍵盤焦點與錯誤狀態。
