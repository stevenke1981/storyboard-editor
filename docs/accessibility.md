# 可及性

- 所有圖示按鈕有 `aria-label` 與 tooltip。
- 所有表單使用可見 label，不只靠 placeholder。
- Shot List 使用 listbox/option 或語意等價結構。
- 鍵盤可完成選取、排序、編輯與播放。
- 焦點不得被 modal 遺失；關閉後回到觸發按鈕。
- 文字與背景對比至少符合 WCAG AA。
- 不以顏色作為唯一狀態提示。
- 動畫尊重 `prefers-reduced-motion`。
