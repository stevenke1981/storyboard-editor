{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal desktop capability for the main Storyboard Editor window",
  "windows": ["main"],
  "permissions": ["core:default", "updater:default"]
}
